apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.healthService.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "health-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.healthService.name }}
  ports:
    - port: {{ .Values.healthService.port }}
      targetPort: {{ .Values.healthService.port }}
      protocol: TCP
