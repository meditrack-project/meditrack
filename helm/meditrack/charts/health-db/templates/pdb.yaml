{{- if .Values.healthDb.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.healthDb.name }}-pdb
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "health-db.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.healthDb.pdb.minAvailable }}
  selector:
    matchLabels:
      app: {{ .Values.healthDb.name }}
{{- end }}
