apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.medicalService.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "medical-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.medicalService.name }}
  ports:
    - port: {{ .Values.medicalService.port }}
      targetPort: {{ .Values.medicalService.port }}
      protocol: TCP
