# ─────────────────────────────────────────────────────────────
# MediTrack — medical-service Deployment
# Environment: both
# Namespace: meditrack-dev | meditrack-prod
# Phase: 2 — Kubernetes + Helm Implementation
# ─────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.medicalService.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "medical-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.medicalService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.medicalService.name }}
  {{- include "meditrack.deploymentStrategy" . | nindent 2 }}
  template:
    metadata:
      labels:
        {{- include "medical-service.labels" . | nindent 8 }}
    spec:
      serviceAccountName: meditrack-app-sa
      terminationGracePeriodSeconds: 30
      {{- include "meditrack.appSecurityContext" . | nindent 6 }}
      {{- include "meditrack.podAntiAffinity" (dict "name" .Values.medicalService.name) | nindent 6 }}
      initContainers:
        {{- include "meditrack.initWait" (dict "name" "medical-db" "port" "5432") | nindent 8 }}
        {{- include "meditrack.initWait" (dict "name" "redis" "port" "6379") | nindent 8 }}
      containers:
        - name: {{ .Values.medicalService.name }}
          image: {{ include "meditrack.image" (dict "registry" .Values.global.registry "image" .Values.medicalService.image "tag" .Values.global.imageTag) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.medicalService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: meditrack-config
          env:
            - name: PORT
              value: {{ .Values.medicalService.port | quote }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: meditrack-db-secrets
                  key: MEDICAL_DB_URL
          resources:
            requests:
              cpu: {{ .Values.medicalService.resources.requests.cpu }}
              memory: {{ .Values.medicalService.resources.requests.memory }}
            limits:
              cpu: {{ .Values.medicalService.resources.limits.cpu }}
              memory: {{ .Values.medicalService.resources.limits.memory }}
          {{- include "meditrack.appProbes" (dict "port" .Values.medicalService.port) | nindent 10 }}
          {{- include "meditrack.lifecycle" . | nindent 10 }}
          {{- include "meditrack.containerSecurityContext" . | nindent 10 }}
