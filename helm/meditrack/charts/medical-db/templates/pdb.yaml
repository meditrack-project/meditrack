{{- if .Values.medicalDb.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.medicalDb.name }}-pdb
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "medical-db.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.medicalDb.pdb.minAvailable }}
  selector:
    matchLabels:
      app: {{ .Values.medicalDb.name }}
{{- end }}
